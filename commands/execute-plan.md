---
description: "Deprecated - use the leyline:subagent-driven-development or leyline:executing-plans skill instead."
---

Tell your human partner that this command is deprecated and will be removed in the next major release. They should ask you to use one of:

- `subagent-driven-development` (preferred) - dispatches a fresh subagent per task with two or three review passes. Recommended for any harness that supports subagents.
- `executing-plans` - batched execution with human checkpoints. Fallback for harnesses without strong subagent support.

Do not proceed with execution from this command. Invoke the appropriate skill.
